<div class="container">
      <% @all_events_pending.each do |event| %>

        <div class="row">
          <div class="col-md-4">
            <h2><%= event.title%> </h2>
            <p> <%= event.description %> </p>
            <p><a class="btn btn-primary" href="<%= event_path(event.id) %>" role="button">Voir l'événement &raquo;</a></p>
            <p><a class="btn btn-success" href="<%= event_submission_path(event.id) %>" data-method="PATCH" role="button">Valider l'événement &raquo;</a></p>
            <p><a class="btn btn-danger" href="<%= edit_event_submission_path(event.id) %>"role="button">Ne pas valider l'événement &raquo;</a></p>

        
          </div>
        </div>
        <br><br>
      <%end%>
        <hr>

      </div> <!-- /container -->

    </main>
